name: Legacy V20 Migration (disabled)

on:
  workflow_dispatch:

# The repository now uses explicit V20 modules. Automatic monolith rewriting is disabled
# to prevent duplicate extraction or accidental changes to the production HTML.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic monolith migration disabled; modular files are maintained explicitly."
