name: Legacy V20 State Boundary Migration (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

# Stage 3 release guard: the legacy state boundary is already present.
# This workflow is retained only as migration history and must not push to main.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy V20 state migration is frozen; use PR -> Stage 3 QA Gate -> merge."
