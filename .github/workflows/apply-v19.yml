name: Legacy V19 Patch (disabled)

on:
  workflow_dispatch:

# V19 is frozen in the current production index.html.
# New gameplay work must go through the modular V20 codebase.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V19 injection workflow disabled; V20 modular migration is authoritative."
