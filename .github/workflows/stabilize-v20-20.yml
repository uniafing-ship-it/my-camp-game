name: V20.20 Stabilization Baseline (frozen)

on:
  workflow_dispatch:

permissions:
  contents: read

# Stage 1 stabilization has been completed. The old self-modifying workflow is
# retained as history only. Stage 3 requires production changes to go through
# PR -> Stage 3 QA Gate -> merge, never a direct workflow push to main.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V20.20 stabilization writer is frozen; the baseline is enforced by Stage 3 QA Gate."
