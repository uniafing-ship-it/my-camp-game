name: Legacy V20 Runtime Wiring (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

# Stage 3 release guard: the V20 module entrypoint is already wired.
# This legacy workflow must never rewrite or push production code directly.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy V20 runtime wiring is frozen; use PR -> Stage 3 QA Gate -> merge."
