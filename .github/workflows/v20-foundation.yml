name: Legacy V20 Foundation Migration (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

# Stage 3 release guard: the foundation migration is historical only.
# Re-running it against the current hybrid runtime could rewrite production files.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy V20 foundation migration is frozen; use explicit modular changes through PR + QA."
